- Allow lists in variables
    - User can set list variables
    - User can load list variables into stack
    - User can use list variable as stack
        - Stack is a variable set as the default stack
        - User can change current stack

- Print time it took to run program when it terminates

- Optimize code
    - Do this when you can't think of new features
    - Likely needs time to be printed

- Allow user to print whole stack with one command

- Methods

- Imports
    - Import methods from other files